# Vercel Bootstrap

The frontend deployment target for Sprint 0 is Vercel. Public and authenticated frontend applications consume the documented API through `@agp/api-client` and must not import backend internals.

No Vercel project or deployment is created by Phase 0.
