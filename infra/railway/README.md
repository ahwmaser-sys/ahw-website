# Railway Bootstrap

The backend deployment target for Sprint 0 is Railway. The repository keeps this provider outside business code by exposing infrastructure only through application ports under `packages/application/src/ports`.

No Railway project, service, variable, or deployment is created by Phase 0.
