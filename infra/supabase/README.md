# Supabase Bootstrap

Supabase is the Sprint 0 target for PostgreSQL, Storage, and Authentication. Supabase-specific code belongs in infrastructure adapters only; domain and application packages must remain provider-independent.

No database migrations, authentication configuration, buckets, policies, or schemas are created by Phase 0.
