// Integration tests setup file
// This file will run before all integration tests
// Useful for establishing database connections or clearing tables

export const setup = async () => {
  // setup db connection pool etc.
};

export const teardown = async () => {
  // tear down db connection pool etc.
};
